# Changelog

## v1.5.2 (2026-09-17)

本次是**市场板块的一次小修 + 一个入口补齐**：成分股能直接看技术分析，
点「详情」不再把整个板块页面弄丢。

### 市场板块：成分股新增「分析」入口

- 成分股列表的「操作」列加了「分析」，与技术分析是**同一个弹窗**：
  评分、自动匹配的交易规则、操作计划（买点 / 止损 / 止盈 / 仓位）、规则回测、支撑压力位
- 与筛选器结果表、推荐榜、自选股的「分析」走**同一条路** —— 不指定规则，
  由后端按这只股票**当前处于什么状态**自动匹配

### 修复：点「详情」后市场板块页面整个消失

- **根因**：`openMemberDetail` 先把市场板块弹窗关掉（`visible = false`），
  指望改用主窗口的详情面板显示。但那个面板本来就被这个模态挡着、根本看不见，
  而弹窗一关，**已选板块、成分股列表、翻到第几页全部丢失** ——
  用户得重新点工具栏图标、重新找到那个板块才能继续看
- **改法**：详情改为弹窗内的第三个视图（板块排行 → 成分股 → 个股详情），
  顶部带「返回成分股」；板块与成分股状态全程保留，返回即回到原处
- 顺带清掉因这次改动而变成死代码的「主窗口详情协调器」——
  `OPEN_STOCK_DETAIL_KEY` 及其 provide / register 三处（只有这一个调用方）

### 质量

- `vue-tsc --noEmit` 与 `vite build` 均通过

## v1.5.1 (2026-09-16)

本次是**去掉一个会误导人的功能 + 补上两处「想用却用不上」的入口**。
主菜是撤掉筹码分布 —— 拿不到权威数据的东西，不如不摆。

### 移除筹码分布（功能下线）

- **筹码分布 / 筹码峰整个去掉**。它算出来跟同花顺等软件完全对不上，而这类图一旦摆出来，
  就会被当成"真实的持仓成本"，实际只是本地模型推演 —— 属于**误导**，所以下线而不是换个说法接着摆
- 查证过程（写下来是为了以后不要重复摸一遍）：
  - **东财没有公开的 CYQ 接口** —— `push2.eastmoney.com/api/qt/stock/cyq/get` 与
    `push2his` 的对应路径**实测都是 404**（同域的资金流 `fflow/daykline` 正常 200，
    所以不是网络问题）
  - A 股**不存在**公开的筹码原始数据：所有软件的"筹码分布"都是本地算的，
    各家换手率口径、衰减系数、价格分桶都不同，互相之间天然对不上
  - 结论：没有"拿到权威数据"这条路可走，就按原计划撤掉
- **「价格位」图保留并改进** —— 去掉筹码图层后腾出的空间补上了**价格刻度轴**，
  一眼能看出各价位离现价多远。图上的每一条现在都来自**价格自己走出来的东西**：
  均线 / 布林轨道 / 摆动高低点 / 前 20 日高低点
- 筹码原来那份权重按各策略的侧重点摊给了摆动点与前 20 日高低 —— 这两路本来就在
  承担"上方抛压 / 下方承接"的语义
- 顺带省掉一次网络请求：个股分析不再拉流通股本（`datasource::profile` 保留但暂无调用方，
  接口已实测可用，将来要做换手率相关的东西可以直接接）
- ⚠️ **同一只股票的支撑/压力位会与 v1.5.0 不同**（少了筹码密集区这一路来源、权重也变了）

### 智能监控：可以「停止」了

- 监控一直只有「删除」，没有「停止」。`Monitor.enabled` 这个字段从 v1.3.0 就在库里、
  也一直参与触发判断，但**没有任何界面能把它翻过来**（开启监控恒写 `true`）
- 现在每行多了「停止 / 恢复」，自选股表右键菜单也能直接切。两者与「删除」的分工：
  - **停止** —— 规则留着，参考价、止损/止盈位、触发状态全都不动，恢复后立刻按原价位继续判断
  - **删除** —— 不再监控这只票了
  - 不能用"删掉再开启"代替停止：那会以**当时的收盘价**重算参考价，止损/止盈整体挪位，
    等于换了一套规则
- 自选股「监控」列新增**已停止**状态（琥珀色），与「监控中 / 已止损 / 已止盈」区分开
- 修一个顺手会踩的坑：**「刷新」不再把停掉的监控悄悄打开** —— 重算价位时保持原有启停状态

### 自选股：分析入口与监控入口都摆到明处

- 「分析」列**固定在表格右侧**。这张表 16 列、横向要滚，之前它会跟着滚出去 ——
  窄窗口下用户根本看不到这里有个按钮，入口等于不存在
- 右键菜单新增**个股分析**与**开启 / 停止 / 恢复监控**，菜单项跟着当前状态变
  （已停掉的票显示「恢复监控」，不会显示「停止监控」）
- 自选股分析与筛选器里的分析走**完全同一条路**（都让后端按个股状态自动匹配规则）

### 全市场筛选器：横向滚动条不再只在最底部

- **问题**：窗口小时整块内容（含筛选条件面板）会一起上下滚，结果表自己的**横向**滚动条
  被推到了可视区之外 —— 想左右挪一下看后面的指标，得先把整块拉到最底，挪完再拉回上面
- **改法**：弹窗内**不再有整体滚动区**，改成每块自己滚，整块永不滚动：
  - 筛选条件面板**可收起**（工具条上新增按钮），收起后把高度全让给结果表
  - 条件面板自身限高并内部滚动；策略 chip 与工具条改为**单行横向滚动**，
    不再折成三四行吃高度
  - 结果表高度改为「量出可用高度 **再扣掉分页条**」（以前没扣，表体 + 分页比容器高出一截，
    正是外层出现滚动条、横向滚动条被挤出去的直接原因）
- 「加自选 / 分析」列同样**固定在表格右侧**，操作类按钮不再需要横向滚动才看得见

### 质量

- Rust 单元测试 **175 项：169 通过 / 6 忽略 / 0 失败**（随筹码一起删掉 9 项，新增监控启停 3 项）
- `vue-tsc --noEmit` 与 `vite build` 均通过

## 更早的版本

> v1.0.0 – v1.5.0。这些功能都还在产品里，所以每个版本只记一句要点；
> 完整说明与各平台安装包见 [Releases](https://github.com/ChineseCanFly-wxy/bull-arrives/releases)。

| 版本 | 日期 | 要点 |
|---|---|---|
| v1.5.0 | 2026-09-16 | 支撑/压力位（权重随规则变）· 交易规则按个股状态自动匹配 · 评分因子精简为 4 个独立维度 · 板块中心 |
| v1.4.0 | 2026-09-15 | 策略可自建可管理（内置 5 → 7 套）· 个股操作计划（买点/ATR 止损/止盈/仓位）· 规则历史回测（扣 0.46% 双边成本）· 自选股「分析」入口 |
| v1.3.3 | 2026-09-14 | 图表在窗口缩放后自适应重绘（PR #3）；自选股与筛选结果接上评分排序 |
| v1.3.2 | 2026-09-14 | 修自动更新在国内网络下静默失效：改为先读系统代理（旧端口表漏了 7897） |
| v1.3.1 | 2026-09-14 | 筛选器改为表格级远程分页，修翻页错位与撑出视口（PR #2） |
| v1.3.0 | 2026-09-13 | **四大模块**：全市场筛选器 / 推荐榜 / 个股量化评分 / 智能监控；数据层重写为多通道回退 |
| v1.2.0 | 2026-09-11 | 分时图补上涨跌幅（按昨收算）；分组快捷键改为录制式并做冲突校验 |
| v1.1.0 | 2026-09-11 | 行情时间限制总开关；休市保留最后行情；悬浮条轮播 / 固定双模式；响应式布局 |
| v1.0.0 | 2026-09-10 | 首个版本：在原项目基础上加自选股分组、持仓管理、行情提醒、桌面提醒窗、偏好设置面板 |

> 本项目基于 [Leaderxin/quant-desktop](https://github.com/Leaderxin/quant-desktop) 二次开发，
> 遵循 PolyForm Noncommercial License 1.0.0（**仅限非商业使用**），详见 [LICENSE](LICENSE)。
