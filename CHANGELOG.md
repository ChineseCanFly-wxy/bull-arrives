# Changelog

## v1.3.3 (2026-09-14)

合并 qioyaz 的 PR #3：修掉图表在窗口缩放后不变形的问题，并给自选股与筛选结果接上量化评分排序。

### 图表自适应

- **窗口尺寸变化后图表重新布局** —— 指数、分时、K 线在缩放 / 最大化窗口后不再被拉变形或留白（`chart.css` 与 `useChartCore` 补上 resize 处理）
- **个股详情与自选列表拆成独立弹性面板** —— 详情不再压住自选列表
- **长自选列表收进表格滚动区** —— 自选多了不再把整个窗口撑高

### 量化评分

- **自选股支持按评分排序** —— 复用已有的评分能力，按总分降序排列
- **筛选结果行可直接打分** —— 不用再逐个点开详情才看得到分数
- **筛选结果支持全局排名** —— 一次筛出的标的统一排座次
- **个股详情可关闭** —— 看完直接收起，不用再找关闭入口

### 质量

- `vue-tsc --noEmit` 与 `vite build` 通过

## v1.3.2 (2026-09-14)

本次为修复版：**修掉自动更新在国内网络下静默失效的问题**。

### 自动更新

- **代理来源改为先读系统设置，不再靠猜** —— 更新客户端以前只按一张写死的端口表（7890 / 10809 / 10808 / 7891 / 1080 / 8118 / 8080）找代理，探不到就直连。现在改为**先读 Windows「Internet 选项」里的系统代理**（`ProxyEnable` + `ProxyServer`，兼容 `http=127.0.0.1:7890;https=127.0.0.1:7897` 分协议写法和 `http://` 协议前缀），探不通才退回端口表
- **端口表补齐** —— 补上 **7897**（Clash Verge Rev 的 mixed 默认端口），以及 7891 / 1087 / 8889 / 8888 / 2080 / 20171 / 10807 / 9910 等常见端口
- **代理来源写进日志** —— 命中系统代理、命中端口表、还是压根没探到，`bull-arrives.log` 里都有明确一行；没探到时会直接提示「国内网络下大概率会超时」，不再让人对着 20 秒超时干瞪眼
- **修掉的实际现象** —— 系统代理端口不在那张表里时（例如 `127.0.0.1:7897`），更新检查会直连 `github.com` 并超时，日志里只有一句 `error sending request`，自动更新从此**静默失效且无法自愈**：应用既升不上去，也永远等不到新版本

### 质量

- 新增 `parse_proxy_port` 单元测试，覆盖普通 `host:port`、分协议写法、协议前缀、空值与非法端口

## v1.3.1 (2026-09-14)

本次为修复版，合并 qioyaz 的 PR #2（`fix/pagination-responsive-layout`），修掉全市场筛选器翻页错位、界面撑出视口等问题。

### 全市场筛选器

- **分页改为表格级远程分页** —— 由 NDataTable 在表格层接管翻页，每次翻页都向后端请求对应的那一页，翻页与明细不再错位
- **分页范围标签修正** —— 正确显示「1–20」，此前显示为「1–19」
- **筛选逻辑保持不变** —— 预设、板块、排除规则、每页条数等记忆逻辑与 v1.3.0 一致

### 布局与界面

- **筛选器卡片与表格限制在视口内** —— 大页尺寸（100 条）下表格内部滚动，窄窗口自动换行，不再把窗口撑出可视区
- **移除重复的每页条数选择器** —— 数据源切换旁那个选择器已删除，每页条数只在设置里配置
- **移除筛选器的构建时间与数据新鲜度时间戳** —— 占位且容易误导

### macOS

- 修复 macOS 设置弹窗的显示问题

### 质量

- 合并后复核：`cargo test --lib --locked` 110 项通过（6 项联网冒烟测试默认忽略），前端 `vue-tsc --noEmit` + `vite build` 通过

## v1.3.0 (2026-09-13)

本次更新的主题：**从「看盘工具」走向「选股助手」**。新增全市场筛选器、推荐榜、个股量化评分与智能监控四大模块，并把数据层重写为多通道自动回退。

### 全市场筛选器（新增）

- **全市场海选** —— 覆盖沪深主板 / 创业板 / 科创板 / 北交所共 5900+ 只，按条件筛选
- **5 套内置预设** —— 全部 / 稳健趋势 / 强势突破 / 短线活跃 / 超跌反弹，一键切换；预设只作用于数值区间，**板块与排除规则独立记忆，切换预设不会被重置**
- **自定义区间** —— 价格、市值、换手率、涨跌幅、量比、成交额自由组合；预设基础上手动微调会自动标记为「自定义」
- **服务端分页** —— 默认每页 20 条（可选 20/50/100，上限 100，设置里可配），先显示命中数量、点「展示数据」再展开明细；翻页逐页向后端取数，后端按「成交额降序 + 代码升序」稳定排序，**翻页不重复、不漏行**
- **条件持久化** —— 预设选择、板块、排除规则、数据源、每页条数全部记住，重启即恢复；关闭筛选器时立即落盘，不走防抖
- **数据源可切换** —— 自动（默认）/ 东方财富 / 新浪财经

### 推荐榜（新增）

- 从全市场**按成交额取最活跃的一批**并发评分，按总分降序排出榜单
- 榜内可直接**加自选**或打开**个股深度分析**

### 个股量化评分（新增）

- **6 因子加权 0–100 分**并给出结论
- 附各因子明细与指标快照（MA / EMA / MACD / RSI / KDJ / BOLL / ATR 等），结论可追溯到具体数据

### 智能监控（新增）

- 在自选股上开启监控后，**按 ATR（真实波幅均值）自动计算止损位与止盈位**，无需手动填价格
- 与原有的价格 / 涨跌幅提醒相互独立，互不影响

### 数据层重写：多通道自动回退

- **全市场快照双通道** —— 东方财富（字段最全，含量比）为主，新浪财经兜底；新浪列表接口按 IP 限流，并发已压至 2 路并在触发风控时立即停止后续请求
- **日 K 三通道** —— 腾讯财经（前复权，主力）→ 新浪财经（覆盖最全，北交所唯一可用）→ 东方财富
- **字段能力建模** —— 数据源不提供的字段（如新浪无量比）时，筛选条件**自动跳过并在界面明示**，不再因拿 0 值比较而筛出空结果
- **分页数据完整性** —— 以「根数够不够」判定通道数据可用性（腾讯对北交所只回 1–2 根的坑），不足时自动换通道，全部不足时返回根数最多的结果
- **一字板判定修正** —— 按板块区分涨跌幅限制：主板 10%（ST 5%）、创业板/科创板 20%、北交所 30%；补上旧版统一 9.8% 阈值漏掉 ST 一字板的问题

### 界面与可诊断性

- **系统页新增构建信息** —— 版本、构建时间、程序完整路径，一眼确认运行的是不是最新版
- **前端日志通道** —— 界面侧的加载失败、筛选结果等会写入日志文件（`bull-arrives.log`），远程排查不用来回截图；失败处均有可点击重试的界面提示
- **筛选器初始化修复** —— 修复对话框以 `v-if` 挂载时初始化逻辑从不执行的致命问题（该问题曾导致预设不显示、条件不恢复）

### 数据源兼容性说明

真机实测发现：东方财富 `clist` / `push2his` 路径在部分网络下会被**间歇性阻断**（同域其他接口正常），新浪列表接口按 **IP 限流**（高频拉取返回 HTTP 456）。因此本版本的数据层以多通道回退为默认策略，并已通过联网冒烟测试验证（详见 `docs/AI接入方案.md`）。

### 质量

- 新增联网冒烟测试（`#[ignore]`，手动触发），可直接区分「数据通道故障」与「前端渲染问题」
- 单元测试 109+ 全部通过

## v1.2.0 (2026-09-11)

### 图表

- **分时图悬停显示涨跌幅** —— 分时图悬停提示补上「涨跌幅」，以**昨收价**为基准计算（交易软件口径），并按红涨绿跌着色。股票详情直接取行情里的昨收；指数详情用「最新价 − 涨跌额」反推。此前分时图只有开高低收与成交量，没有涨跌幅

### 快捷键

- **分组切换快捷键改为录制式** —— 点击方框后直接按组合键即可录制，不再需要手打 `Ctrl+Alt+Left` 这类字符串；支持一键清空，留空即停用该功能（清空后不再注册，也不占用系统快捷键）
- **上传前冲突校验** —— 保存依次校验：必须含修饰键 → 系统保留组合（⌘Q / ⌘W / Win+L 等）→ 与另一方向重复 → 与悬浮窗快捷键重复 → 交系统注册，被占用时直接报中文错误且旧快捷键保持不动
- **生效状态可见** —— 设置面板回读真实注册结果，分别显示「已生效」或「未生效（可能被其他程序占用）」，不再出现「保存成功却没反应」
- **macOS 适配** —— 录制到的 ⌘ 正确映射为 Super；快捷键统一按 ⌃⌥⇧⌘ 符号展示（悬浮窗快捷键同步），并提示避开系统快捷键

## v1.1.0 (2026-09-11)

### 行情获取

- **时间限制总开关（默认关闭）** —— 关闭时全天获取行情，盘前/午休/休市自动降低轮询频率；开启后才按下方配置的时段与休市日拦截请求。旧版本已保存的时段配置会保留，升级后默认不生效
- **休市保留最后行情** —— 新增指数行情持久化缓存，休市或重启后指数不再空白，与股票一样展示最后一次有效报价；时间限制生效期间不再清空界面，只停止联网
- **防止行情回退** —— 缓存更新增加时序校验，旧响应不会覆盖已更新的报价

### 界面

- **主页面响应式** —— 修正窗口缩小时的布局裁切；窗口变矮时 K 线不再被固定高度挤掉，详情区域在窄窗口转为上下排列，矮窗口可在详情内部滚动
- **图表自适应** —— 图表高度改为随窗口变化，并接入尺寸监听，拖动窗口时画布实时重绘；补全缺失的加载、错误与重试遮罩样式
- **K 线悬停提示** —— 提示框改为固定展示，贴近图表边缘或窗口较小时也完整可见；补回成交额字段。涨跌幅按相邻 K 线收盘价计算
- **GitHub 入口** —— 状态栏常驻 GitHub 仓库按钮，支持键盘聚焦，打开失败不影响应用

### 悬浮行情条

- **轮播 / 固定两种模式** —— 轮播模式可自定义每页展示数量（1–20 只）；固定模式一次展示当前分组全部股票，不再自动翻页
- **窗口高度自适应** —— 悬浮条高度按行数自动调整，超出屏幕时内部滚动，拖动位置始终保持在可见区域内

### 其他

- 合入 [PR #1](https://github.com/ChineseCanFly-wxy/bull-arrives/pull/1)（作者 [qioyaz](https://github.com/qioyaz)）：macOS 点击 Dock 图标恢复主窗口、休市期间自选股搜索可用、更新代理探测改为 HTTP CONNECT 握手校验（避免误用 8080 等非代理服务）

## v1.0.0 (2026-09-10)

Bull Arrives 首个版本。

### 基础能力

继承自原项目并保留：

- **实时行情** —— 腾讯证券（默认）/ 新浪财经（备用）双数据源，一键切换
- **七大指数** —— 上证、深证、创业板指、科创 50、科创 100、中证 500、科创综指
- **图表分析** —— 分时图 + K 线（1/5/15/30/60 分、日/周/月），主图 MA / BOLL，副图 VOL / MACD / KDJ / RSI
- **五档盘口** —— 买卖五档，静默刷新
- **浮动行情条** —— 始终置顶、自动轮播、拖拽定位、透明度与单色显示
- **系统托盘常驻** —— 关闭窗口不退出，支持开机自启
- **自选股管理** —— 代码 / 中文名 / 拼音缩写搜索，排序与右键菜单
- **离线缓存** —— 启动即恢复上次报价，无需等待网络
- **自适应轮询** —— 交易时段感知，盘前/午休/休市自动降频
- **便携模式** —— 放置 `portable.dat` 即切换为绿色版

### 本版本新增

- **自选股分组** —— 新建/重命名/删除分组，组内独立排序，全局快捷键循环切换
- **持仓管理** —— 录入每股成本价与持仓股数，实时计算持仓市值、浮动盈亏与盈亏比例
- **行情提醒** —— 涨跌幅提醒（按昨收价）+ 固定价格提醒（向上/向下穿越触发）；支持**每日一次 / 重新穿越 / 冷却间隔**三种重复方式，规则可暂停而不删除
- **桌面提醒窗** —— 独立置顶提醒窗口，多条自动排队，点击跳转主窗口
- **提醒记录** —— 保留本次运行最近 50 条，可查询与清空
- **偏好设置面板** —— 集中管理行情（刷新间隔、获取时段、休市日）、提醒、悬浮窗、系统设置

### 说明

- 本项目基于 [Leaderxin/quant-desktop](https://github.com/Leaderxin/quant-desktop) 二次开发，原作者 **Leaderxin**，遵循 PolyForm Noncommercial License 1.0.0（**仅限非商业使用**）
- Windows / Linux 支持**应用内自动更新**，无需手动下载新版本
- 数据目录：标准安装 `%APPDATA%\bull-arrives\`，便携模式 `<exe 同级>/data/`
