# Changelog

## v1.5.0 (2026-09-16)

本次两方面：**看盘时能拿到更完整的判断依据**（板块、筹码、关键价位），
以及**分析不再一刀切**（交易规则按个股自己的状态匹配）。
同时把评分体系里重复计权的部分清理掉了。

### 筹码分布与支撑压力位（新增）

- **筹码分布（估算）** —— 按日 K 与换手率推算的持仓成本分布：获利盘比例、平均成本、
  90% / 70% 成本区间与集中度、筹码峰。换手率优先取数据源直接给的，
  拿不到就用「成交量 ÷ 流通股本」反推，**因此不需要新增数据源**
- **支撑位 / 压力位** —— 来源包括均线、布林轨道、摆动高低点、前 20 日高低、筹码峰，
  **按当前交易规则加权**：趋势跟随看均线（回踩 MA20 是买点）、均值回归看布林轨道、
  放量突破看前高与上方筹码峰（前高之上压着大额套牢盘，突破就很难成立）
- 分析弹窗新增「价格位」图，把筹码分布与价位叠在同一根价格轴上
- ⚠️ 界面明确标注：筹码是**模型估算**，与各券商软件的口径不同，不必强求一致

### 交易规则：按个股状态自动匹配（重要变化）

- 打开个股分析时**自动挑一条最合适的规则**。依据是这只股票**当前处于什么状态** ——
  均线走好就是趋势状态、跌进超卖就是超跌状态、破了前高就是突破状态，
  而三条规则各自的入场条件本身就是状态判据
- 界面同时列出三条规则各自「满足 / 未触发」，可以手动切换，也可以切回自动
- **刻意不按「历史上哪条规则赚得多」来挑**：实测那样做的选对率只有 40%
  （随机挑是 33%），本质是在 3 个噪声里挑最大值
- ⚠️ **评分口径随之调整**：因子从 6 个精简为 **4 个彼此独立的维度**
  （趋势 / 量能 / 动量 / RSI 极值）。删掉了与趋势因子相关 0.71 的 MACD、
  判据只有 4 档的 KDJ；RSI 改为只判超买超卖极值（旧版奖励的「50~70 多头区」
  其实是趋势因子在说的话）。量能是唯一与其余因子零相关的维度，权重从 15% 提到 25%。
  **同一只股票在本版的分数会与 v1.4.0 不同**

### 板块中心（新增，感谢 @qioyaz）

- 行业 / 概念板块列表与成分股
- 修正东财 `clist` 字段映射，并增加 `push2delay` 主机与重试兜底

### 全市场筛选器

- 结果表**直接显示入场条件是否成立**（满足 / 未触发），不必逐只点开分析
- 弹窗支持**拖拽缩放**，尺寸自动记忆；窗口变小时尺寸跟着收敛，不会跑到屏幕外
- 全市场评分排序改用批量接口，一次 IPC 出一批，速度明显提升

### 修复

- **东财日 K 把「成交额」当成换手率填** —— 真实换手率在第 11 个字段
- **东财日 K 的成交量单位与另两个通道差 100 倍** —— 它被换成了「股」，
  而腾讯 / 新浪是「手」。量比是比值所以一直没暴露，加筹码分布时才炸出来
- 推荐榜弹窗未导入 `NModal` 导致的透明幽灵层与错位（感谢 @qioyaz）
- 自选股表格新增「监控」列，显示监控中 / 已止损 / 已止盈（感谢 @qioyaz）
- macOS 全屏状态点关闭按钮黑屏（感谢 @qioyaz）

### 文档

- `docs/AI接入方案.md` 随仓库发布 —— 此前仅在本地维护，README 里的链接点不开
- README 的「规划中」扩充为可直接照着推进的待办清单，第一项即 AI 接入方案

### 质量

- Rust 单元测试 **175 项通过**（v1.4.0 为 140 项）
- `vue-tsc --noEmit` 与 `vite build` 均通过

## v1.4.0 (2026-09-15)

本次是**策略与操作层面**的一次大更新：筛选器里的策略可以自己增删改查了，
个股分析也不再只给一个分数 —— 还给能照着下单的买点、止损、止盈。

### 策略可自建可管理

- **策略增删改查** —— 在筛选器里直接把当前条件存成自己的策略：新建、重命名、用当前条件覆盖、另存为副本、删除。内置策略只读（随版本维护），点「另存为我的策略」即可改造成自己的
- **内置策略 5 套 → 7 套**，并修正了两条名不副实的：
  - 「稳健趋势」补上「近 60 日不弱」—— 此前它的条件全是当日字段，**没有任何趋势判定**
  - 「超跌反弹」改成真正的超跌（近 60 日跌超 15%）—— 此前只判当日跌幅，高位回调 1% 的股票也能入选，而真正腰斩的票只要当天平盘就落选
  - 新增「低估值价值」（以市净率为主约束 + 只保留正盈利 —— 低 PB 是 A 股最稳健的估值因子）
  - 新增「低波动稳健」（振幅上限 + 中大盘）
  - **刻意不做小市值策略** —— 2024 年退市新规后，微盘股的风险已不是收益因子能覆盖的
- **筛选条件扩展** —— 新增 60 日涨跌幅、市盈率、市净率、振幅上限四组条件
- **策略条常驻显示实时条件摘要** —— 光看四个字的策略名，不知道它到底筛什么
- 保存策略时会自动纠正「最小值填得比最大值大」的写法，不会存下一个恒为空集的策略

### 操作计划：不止选股，还给出买卖点

- 个股分析新增**操作计划** —— 建仓区间、止损价、止盈价、盈亏比、按单笔风险反推的仓位上限，每条价位都写明是怎么算出来的；当前不满足入场条件时直接说「还差什么」
- **三套交易规则**，各自说明自己的性格：均值回归胜率能过六成但盈亏比通常不足 1.5；趋势跟随胜率约四成但盈亏比可到 2 以上；放量突破介于两者之间
- **界面不允许只显示胜率** —— 公开资料里有「胜率 66.7% 但一年只赚 1.06%」的实例，赢的次数多不等于赚钱，所以胜率、盈亏比、每笔期望必须放在一起看
- **策略自带交易规则** —— 每条筛选策略配套一条规则（超跌反弹配均值回归、强势突破配放量突破…），打开个股分析时自动用对应规则算买点。筛选器只有单日快照，没有均线与 RSI，精确买卖点必须逐只看日 K

### 规则历史回测

- 同一套规则在这只股票**自己的日 K** 上跑一遍，给出触发次数、胜率、盈亏比、每笔期望、最大回撤、平均持仓 —— 而不是引一个别人文章里的数字
- **扣掉 0.46% 双边交易成本后再判输赢** —— 一笔 +0.3% 打到止盈的，扣完成本其实是亏的
- 同一根 K 同时触及止损与止盈时**按止损先成交**（保守假设）
- 样本不足 10 次会明确提示「基本是噪声，别当依据」，不谎报统计意义

### 自选股

- 每行新增「分析」入口 —— 此前自选股只有整表评分排序，看不到单只为什么是这个分，更看不到买卖点

### 质量

- `cargo test --lib --locked` 140 项通过（新增 14 项，含「预设必须真的用上中期维度」的回归守卫）
- `vue-tsc --noEmit` 与 `vite build` 通过

## 更早的版本

> v1.0.0 – v1.3.3。这些功能都还在产品里，所以每个版本只记一句要点；
> 完整说明与各平台安装包见 [Releases](https://github.com/ChineseCanFly-wxy/bull-arrives/releases)。

| 版本 | 日期 | 要点 |
|---|---|---|
| v1.3.3 | 2026-09-14 | 图表在窗口缩放后自适应重绘（PR #3）；自选股与筛选结果接上评分排序 |
| v1.3.2 | 2026-09-14 | 修自动更新在国内网络下静默失效：改为先读系统代理（旧端口表漏了 7897） |
| v1.3.1 | 2026-09-14 | 筛选器改为表格级远程分页，修翻页错位与撑出视口（PR #2） |
| v1.3.0 | 2026-09-13 | **四大模块**：全市场筛选器 / 推荐榜 / 个股量化评分 / 智能监控；数据层重写为多通道回退 |
| v1.2.0 | 2026-09-11 | 分时图补上涨跌幅（按昨收算）；分组快捷键改为录制式并做冲突校验 |
| v1.1.0 | 2026-09-11 | 行情时间限制总开关；休市保留最后行情；悬浮条轮播 / 固定双模式；响应式布局 |
| v1.0.0 | 2026-09-10 | 首个版本：在原项目基础上加自选股分组、持仓管理、行情提醒、桌面提醒窗、偏好设置面板 |

> 本项目基于 [Leaderxin/quant-desktop](https://github.com/Leaderxin/quant-desktop) 二次开发，
> 遵循 PolyForm Noncommercial License 1.0.0（**仅限非商业使用**），详见 [LICENSE](LICENSE)。
