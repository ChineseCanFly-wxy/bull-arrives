# Changelog

## v2.2.4 (2026-09-26)

修掉「打开软件一片空白」——根因是前后端的**启动竞态**，不是界面代码渲染坏了。

### 打开软件空白：窗口先于后端就绪，初始化第一步就失败

- 现象：双击打开后界面停在空白，没有内容、也没有可操作提示，等多久都不会自己恢复
- 根因：Tauri 会**先创建窗口、再执行后端 `setup`**。前端一加载就调 `get_settings` 读设置，
  而 `db` 这个全局状态在后端要等 `Database::open` → 数据源初始化 → 缓存从 SQLite 恢复
  整串跑完才注册。这段窗口期里前端的调用会直接失败（`state not managed for field db`），
  而初始化第一步失败会让后面的自选股加载、行情订阅、交易日历全部不执行 —— 界面于是停在空白
- 为什么 v2.2.3 才暴露：这一版在 `setup` 里新增了工作目录长名规范化（`canonicalize`）等
  文件系统调用，把这段窗口期拉长到足以让前端稳定抢先，原本偶发的问题变成了必现
- 修复分两层：
  - **后端把窗口期压到最短** —— `db` 改成在 `Database::open` 之后**立刻注册**，
    不再排在一整串初始化之后；注册点保持唯一，避免重复注册触发 panic
  - **前端等后端就绪** —— `get_settings` 增加启动重试（最多 30 次 × 200 毫秒），
    只对「状态尚未注册」这类竞态特征串重试，其他错误照旧立刻上报；
    匹配条件放宽成整句特征，避免 Tauri 日后换文案导致重试静默失效

### 启动过程不再像「假死」

- 初始化未完成时显示「正在连接本地服务并加载设置…」，把「还在启动」和「真的卡死了」区分开
- 提醒组件改为初始化完成后才挂载，避免它在后端尚未就绪时抢跑

### 验证

- `cargo test --lib`：**254 通过 / 0 失败 / 20 忽略**（忽略项是需外部环境的手动测试）
- 前端类型检查与生产构建通过（`vue-tsc --noEmit` + `vite build`）

## v2.2.3 (2026-09-25)

把「规划中」里的两条待办落地：AI 解读不再是「等它跑完给一份报告」，而是**把终端打开直接对话**；
设置「改了、退出再进来又变回去」的根因也抓到了。另外新增一套可切换的界面风格。

### AI 解读改成「交互终端」：能追问、能取消、能接回

- 之前是后台一次性任务：90 秒硬超时 + 单次预算封顶，跑完给一份结论；中途想追问细节只能整个重跑，
  超时或额度用尽还会直接中断 —— 也就是「有问题只能自己去 Claude 页面看」
- 现在多了一条**交互终端**链路：会话真正跑起来，边跑边把事件流推到界面（滚动保留最近 240 条），
  可以随时追问、中途取消；关掉窗口后按任务 id **重新接回**同一个会话，上下文不丢
- 交互终端由用户直接操作，**不套用后台那次 90 秒超时，也不占用单次预算**；
  后台自动任务（个股解读 / 多角色研判 / 资讯摘要 / 动态筛选）仍照旧受预算约束
- 交互任务落在数据目录下的 `agent-interactive\<任务 id>\`，可列出、查看活动状态、导入、删除；
  读写时校验目录与记录文件的类型、符号链接与大小上限（记录 4 KB、输出 64 KB）
- 实时链路用 `--output-format stream-json --include-partial-messages` 解析；
  单轮上限 5 分钟、单行 256 KB、单次输出累计 2 MB，超限即截断而不是把内存吃满
- 新增 10 个命令：`start_interactive_analysis` / `start_live_analysis` / `ask_live_analysis` /
  `get_live_analysis` / `cancel_live_analysis` / `list_interactive_analyses` /
  `resume_interactive_analysis` / `import_interactive_analysis` / `delete_interactive_analysis` /
  `inspect_interactive_analysis`

### 新增「后台单次预算」设置项

- 单次预算原本是写死的，现在可以在设置里改：**0.05–10 美元，默认 0.20**
- 保存时做范围与有限性校验；数据库里存了非法值（`NaN`、超范围）时**回落到默认 0.20**，
  不让一个坏值把整条 Agent 链路卡死；有回归测试 `agent_budget_is_bounded_and_configurable` 守着
- 设置面板把口径写清楚了：这项预算**只管应用发起的后台调用，不是账户余额**，
  交互终端由用户直接操作、不套用这条预算

### 修掉「设置改了、退出再进来又变回去」

- 现象：改透明度、单色、字体颜色这类设置，当时生效，退出重进就被重置
- 根因是**竞态**：`get_settings` 是异步读回来的，如果用户在读取返回**之前**就改了某项，
  随后到达的那份**旧快照会把刚改的值覆盖掉** —— 手快的设置最容易中招，看起来就像「没保存」
- 修法：给每个设置项加**修订号**，读取时只接受「本次读取开始之后被本地改过」的值，
  旧快照再也盖不掉新改动
- 悬浮窗透明度 / 单色 / 字体颜色：**窗口还没创建出来也先落库**，窗口重建时从数据库恢复；
  以前窗口不存在就直接返回成功但什么都不存
- 开机自启：改成「先落库、再动系统」，系统调用失败会**把数据库回退**，
  不再出现「设置显示已开、实际没开」的半截状态，报错文案也会说明是回退成功还是回退失败
- 设置面板补上防误操作：保存过程中不允许关闭、有未保存修改时先确认、每个开关各自独立的进行中状态
- 读取失败分级：核心设置读失败才报错；数据源列表 / 自启状态 / 便携模式读失败只记 warning，
  不再因为一个附属项失败就把整个设置面板判成「加载失败」
- 后端补上入参校验：`theme` 只能 light / dark，`visual_style` 只能 classic / trading / modern，
  非法值直接拒绝落库

### 新增界面风格：经典 / 专业 / 现代

- 设置 → 外观里新增「界面风格」三档（classic / trading / modern），与明暗模式**互相独立**，
  两者都可自动保存、下次启动恢复
- 实现方式是在根元素上挂 `data-style`，新增 `chart-switcher.css`、`workspace.css` 两套外壳样式；
  **classic 保持原样不动**，新风格只覆盖弹窗、图表切换器这些共享 chrome，避免影响既有布局
- 图表切换器（主图叠加 / 副图指标）在三种风格下各自有对应形态

### 退出流程更干净

- 退出时先取消所有在跑的 AI 会话，再**等行情调度真的停下来**（最多 8 秒），
  避免退出瞬间缓存还没落盘就没了；等待超时只记 warning，不会卡住退出
- 行情调度新增可唤醒的停止通知机制，退出不用靠轮询空转

### 其他修复与优化

- stockdb 状态：引擎 / 更新器路径未配置时，回退按「已配置的引擎」推断路径，不再显示为空
- 行情监控：行情索引改成先建一次 HashMap，不再让每条提醒各自线性查找一遍
- 前端构建分包：klinecharts、naive-ui 数据表、Vue 运行时、工具库拆成独立 chunk，
  减少四个窗口之间的重复加载
- 修掉测试对具体日期的依赖：交易日历断言改用周末判断，避免并行测试互相干扰

### 验证

- `cargo test --lib`：**254 通过 / 0 失败 / 20 忽略**（忽略项是需外部环境的手动测试）
- 前端类型检查与生产构建通过（`vue-tsc --noEmit` + `vite build`，新分包生效）
- 新增回归测试：预算范围与非法值回落（`agent_budget_is_bounded_and_configurable`）·
  交互任务重开后仍可用、且不会导入未校验的输出 · 活动状态拒绝超长输出 ·
  被改过的输入无法续跑 · 设置重开数据库后仍然存活

## v2.2.2 (2026-09-20)

修掉「测试连接」以及**所有 Agent 功能**在 Windows 上被 Claude Code 判为「权限拒绝」的根因，
并把两处需要手输路径的地方改成可以浏览选择。

### 修掉 Agent 工作目录踩到 8.3 短名导致整条调用链被拒

- 现象：点「测试连接」报 `Claude Code 权限拒绝，请检查其管理策略是否允许读取任务文件和写入结果
  （退出码：exit code: 0）`；个股解读、多角色研判、资讯摘要、动态筛选同样跑不通
- 根因**不在 Claude Code 的权限配置**：应用每次调用都会新建 `%TEMP%\bull-arrives-agent\<随机>` 当工作目录，
  而 Windows 的 `%TEMP%` 可能被写成 **8.3 短名**（形如 `C:\Users\WEIXY4~1\AppData\Local\Temp`）。
  这种路径下 Claude Code 的 `--allowedTools` 放行规则匹配不上，`dontAsk` 模式把 Read / Write 一律判为拒绝，
  模型只能回报「被拒绝」并以**退出码 0** 正常结束，应用因此收不到 `response.json`
- 修法：工作目录一律先**规范化成长名路径**（`canonicalize` 后去掉 `\\?\` 前缀）再使用；
  自动模式下还会复检一次，确实拿不到长名时退到应用数据目录下的 `agent-runs\`，都不行才明确报错
- 实测对照（同一套启动参数，只换工作目录）：短名目录 4/4 失败，长名目录全部通过；
  `--add-dir` 指到长名目录、把放行规则写成**长名绝对路径**，两者都绕不过 —— 只有换掉工作目录有效
- 新增「工作目录」设置项：默认**留空自动选择**（推荐），也可以「浏览…」指定一个文件夹；
  保存时校验目录存在、可写、不含 8.3 短名。设置里直接显示任务文件**实际写在哪里**

### 可执行文件改为可以浏览选择

- 「Claude Code 可执行文件」不再要求手输路径：**「浏览…」直接选文件**，选完立即校验并刷新连接状态；
  留空依然自动检测，两条路都保留

### 顺便修掉一处会把人带偏的报错文案

- 分类提示是**按输出关键词猜的**：模型自己说「我被拒绝了」也会被归到「权限拒绝」，
  而且「权限拒绝」与「退出码：exit code: 0」同时出现自相矛盾 ——
  现在工作目录不可用时会提前明确报出来，不再让人去 Claude Code 的权限策略里找答案

### 验证

- `cargo test --lib`：**237 通过 / 0 失败 / 19 忽略**（忽略项是需外部环境的手动测试）
- 新增回归测试：8.3 短名识别、`\\?\` 前缀剥离、自动工作目录不含短名段、手动工作目录校验与创建
- 真实链路冒烟（走应用自己的启动器 + Job 门控 + 真实模型调用）：`local_claude_connection_smoke` 通过
- 前端类型检查与生产构建通过

## v2.2.1 (2026-09-19)

v2.2.0 的修补版：三处实际使用中暴露出来的问题。

### stockdb 启动不再弹浏览器 / 托盘

- 拉起 stockdb 时固定带 `-d`（它的官方选项 `-d: run as daemon`）。不带这个参数它走桌面模式 ——
  建托盘图标并用 `ShellExecuteA` 打开自己的网页，于是应用每次自动管理都会弹浏览器和托盘
- 顺带说明：它 `-h` 给出的选项只有三个（`-d` / `-s start|stop|restart` / `-h`），
  **除 `-d` 之外没有别的「不弹浏览器」开关**；配置默认读引擎同目录的 `stockdb.conf`
- 这条约束固化成单测 `engine_is_always_started_as_daemon`：断言启动参数里必须有 `-d`，
  以后谁把它「简化」掉会立刻测试失败；全项目只有一个启动引擎的入口，没有旁路

### 全市场筛选器：结果表换成「行业 + 概念」

- 移除「板块（沪/深/创/科/北）」与「上市天数」两列 —— 板块看代码前缀就知道，占一列不值
- 新增「行业」+「概念」两列：数据取东财快照自带的字段（`f100` / `f103`），
  与其它字段同批返回，**不产生任何额外请求**，用来回答「这只票是走什么概念在涨/跌」
- 一只票通常同时属于多个概念：只展示前两个、其余折叠成 `+N`，整串放进悬停提示，
  单行高度恒定，不会把后面的列挤出可视区
- 新浪通道没有这两个字段：新增通道能力位，界面会明说「换个通道才有」，而不是默默显示 `--`

### 全市场筛选器：小窗口下看不到横向滚动条

- `scroll-x` 不再写死 1224，改为**按列宽求和** —— 列宽一改就与真实宽度脱节，
  会出现「滚动条拉到底也看不全后面的东西」
- 表体高度要扣掉的分页高度改为**量真实 DOM**：窄弹窗下分页控件会换行变高，
  原来估的常量偏小，表体 + 分页撑破外层容器，而外层是 `overflow: hidden`，
  正好把表体底部那条横向滚动条裁掉 —— 表现就是「窗口小时下面没有左右滚轮」

### 验证

- `cargo test --lib`：**232 通过 / 0 失败 / 19 忽略**（忽略项是需外部环境的手动测试）
- 前端类型检查与生产构建通过

## v2.1.1 (2026-09-18)

本次两件事：**悬浮条上直接管自选股**（不切到主界面就能加 / 删），以及把**本地历史数据引擎
接管起来** —— 设置里选一次引擎，之后随应用自动启停、顶栏一键更新数据；
另外在 README 里正式标注了数据引擎的出处。

### 悬浮窗：不用打开主界面就能加 / 删自选股

- 悬浮条右侧新增 **＋** 入口。它平时是隐藏且**不可点击**的 ——
  整条悬浮条都是「点击打开主界面」的热区，放一个看不见但能点的按钮必然误触，
  所以只在鼠标悬停时才露出来
- 点开后是一个贴着悬浮条弹出的「快速自选」小面板：
  - **加**：输入代码或名称即搜（复用全市场搜索的新浪 → 腾讯 → 数据源三级回退），
    点结果就加入；**回车直接加第一项**，输代码时基本不用碰鼠标
  - **删**：当前自选列表每行一个 ×，一键移除
  - **看**：面板一收起，悬浮条上就有这只票的行情
- 典型用法就是「马上看一眼某只票 → 看完直接删掉」，全程不需要主界面
- **为什么是独立小窗，而不是直接在悬浮条上做**：悬浮条是 `WS_EX_NOACTIVATE` 的
  非激活工具窗 —— 这正是它「点了不抢焦点、像不存在一样」的原因 —— 代价是
  **它拿不到键盘焦点**，上面放输入框是打不了字的。新窗口只在需要时弹出，
  悬浮条原有的拖拽 / 点击 / 不抢焦点行为一行没动
- 面板失焦自动收起，也可以按 `Esc` 或点右上角 ×；增删完悬浮条与主界面都会自动刷新
  （后端本来就发 `watchlist-changed`，两条界面都在监听）

### 本地历史数据：自动管理 stockdb

- 新增独立的 stockdb 管理器：**自动扫描候选 / 手动浏览选择**引擎（`stockdb.exe`）与
  更新程序（`数据更新.exe`），并负责启停与状态上报（`stockdb-status-changed` 事件）
- **默认关闭**：不开就**不启动、不探测、不读取** stockdb，实时行情 / 盘口 / 监控 / 提醒
  一律不受影响。开启后**随应用启动自动拉起**已选的 `stockdb.exe`
- **只管理自己拉起的实例**：关到托盘不停服务；从托盘真正退出时才关闭它。
  如果你已经手工启动了服务，它会直接使用，但**不接管、也不结束**你的进程
- 顶栏设置按钮旁新增「更新本地数据」：先停掉自己启动的 stockdb →
  弹出同目录 `数据更新.exe` 的原生窗口 → 更新程序退出后自动把服务拉回来。
  更新期间按钮转圈并禁用，此时从托盘退出会被拦下并提示等待
- 设置面板里能看到探测结果、候选列表与当前服务地址（默认 `http://127.0.0.1:7899`）
- 平台说明：**当前只有 Windows 支持自动管理**（引擎本身是外部程序，不含在安装包里）
- 为此引入 `tauri-plugin-dialog`（用于选文件），capabilities 补 `dialog:allow-open`

### 文档

- README 补上**本地历史数据引擎**的引用：**[hello245m/free-stockdb](https://github.com/hello245m/free-stockdb)**，
  写法与原作者的署名保持一致（顶部说明 + 致谢段 + 许可证），并写明它是以
  **独立本机服务**的形式对接 —— 本仓库不分发它的代码或数据，是否启用由用户决定
- 原因是这块能力此前只在版本说明里提了一句「可配 stockdb 地址」，没交代它来自哪个项目、
  什么许可，对使用者不够清楚

### 质量

- Rust 单元测试 **214 项：200 通过 / 14 忽略 / 0 失败**
- `vue-tsc --noEmit` 与 `vite build` 均通过
- ⚠️ 新窗口必须同时在 `src-tauri/capabilities/default.json` 的 `windows` 里登记，
  否则它的窗口 / 事件 API 会被能力系统拒绝（悬浮条、提醒窗当年也是这么加的）

## v2.0.0 (2026-09-18)

本次把应用从「看盘 + 量化评分」推到两条新主线上：**让本机的 AI 参与解读**，
以及**让策略能被模拟验证、并持续淘汰**；同时补齐板块中心、盘中资讯、本地历史数据三块。
所有 AI 与模拟功能**默认关闭**，不开就完全不会联网，也不会动到账本。

### 本地 Agent（Claude Code）

- 应用**不直连模型、不管理密钥**：改为驱动本机已装的 Claude Code，输入只有代码算出来的量化快照。
  这样不必维护第二套模型配置，也直接复用你本机已有的 agent 能力
- **权限是显式收敛的**：只用 `--permission-mode`，**明确禁止 `bypassPermissions`**（有测试守着），
  工作目录隔离；**不读取、也不改动** `~/.claude` 的全局配置。固定单并发、90 秒超时、可中断并回收整个进程树
- 检测不到可执行文件时**给明确指引**，而不是静默失败；agent 失败时只保留纯量化结果，不假装有 AI 结论
- 两个入口：单次单角色解读，以及**多子代理团队分析**（技术 / 资金 / 资讯等角色各跑一个进程再汇总）
- 每次预测落库，**次日自动比对**给出真实校准曲线 —— 不靠"感觉准"

### 模拟买卖与胜率测算

- 独立的模拟账户（与真实持仓数据分开）；下单来源分**手动 / 规则 / AI** 三种，**每笔都记触发来源** ——
  否则事后答不出「这个数字是哪条策略跑出来的」
- 成本集中到可配的 `FeeConfig`，**不再是写死的单一常数**
- **只允许用未复权行情成交**：本地历史库目前只有前复权 K 线，这种情况下自动任务**只生成指令、
  不改动账本**。用复权价撮合会把涨跌停判定和成本一起算错，这是最容易埋进去的一个坑
- 拒绝路径覆盖：停牌、涨跌幅限制未知（**宁可阻断也不错误成交**）、一字 / 开盘涨跌停、
  **T+1 可用持仓**、可用资金不足；自动下单按 **100 股整手**取整

### 策略卡片与闭环

- 策略**先登记成卡片才能进入验证**，不许「口头策略」直接跑；每次修改留版本，可回溯
- 六段流水线**不可跳不可省**：静态校验 → 因果审计 → 历史回看 → 准入门槛 → 实时前向 → 考核期；
  **阶段没全部通过，就不允许转成「在用」**
- 策略库固定展示 **试用 / 淘汰 / 在用** 三个计数 —— 试得越多、挑出来的越好看，
  这是多重检验偏差在闭环里的具体形态，必须摆在明面上
- 在用策略可降级 / 暂停；**淘汰只退役不删除**，保留全部历史与每一次状态变更的原因

### 因果审计与因子研究

- 因果审计 = 静态字段 / 算子 / 窗口校验 + **前缀重算对比**（全量结果与「只截到 t」的结果必须一致）。
  此前文档声称「不含未来函数」却没有任何测试证明，现在有了
- 信号实现与操作计划、回测**共用同一份**，避免三处各自漂移
- 新增因子研究：分位收益与正收益概率分布，用来判断一个信号到底有没有预测力

### 板块中心

- 板块 K 线；涨停家数（**明确标注为非官方派生**，算不出来就写「不可用」，不编数）
- 主力净流入 / 净占比、近 3 / 5 / 10 日涨跌幅
- 资金流与轮动视图：强势 / 弱势 × 流入 / 流出四象限 + 批量资金流排序
- 资金流字段本次没返回时如实提示，**不影响**板块排行与成分股

### 盘中资讯（规则版）

- 独立轮询（东方财富上市公司快讯 + 公司公告），**默认关闭，关闭时零请求**
- 交易日按关注池匹配、类型白名单分级；**首次开启只建立当前水位**，不补推历史内容
- 命中后尝试 Agent 摘要，失败回退原文

### 本地历史数据（stockdb）

- 设置里可配服务地址与引擎目录，支持扫描与连通性测试
- 长周期分析**优先读本机 stockdb**；不可用时单股分析回退在线短区间，并**如实标注天数与来源**
  （在线通道 640 根左右就封顶，本地是二十多年，两种来源不能混着说）

### 其他

- NSIS 安装模式固定为 `currentUser`（不再需要管理员权限）
- 新增依赖 `sha2` / `hex` / `rmp-serde`；移除已无引用的 `scripts/make-latest-json.mjs`

### 质量

- Rust 单元测试 **212 项：198 通过 / 14 忽略 / 0 失败**
- `vue-tsc --noEmit` 与 `vite build` 均通过

## 更早的版本

> v2.2.0 与 v1.0.0 – v1.5.3。这些功能都还在产品里，所以每个版本只记一句要点；
> 完整说明与各平台安装包见 [Releases](https://github.com/ChineseCanFly-wxy/bull-arrives/releases)。

| 版本 | 日期 | 要点 |
|---|---|---|
| v2.2.0 | 2026-09-19 | Claude Code 真正接进应用（Agent 工作台 / 主动测试连接 / 15–300 秒超时）· 修掉「冻结快照指纹校验失败」· 新增研究中心（想法 → 前向证据台账）· 模拟撮合补实时前向 · 交易日历自行推断 · A 股交易规则按板块收口 |
| v1.5.3 | 2026-09-17 | 智能监控可删除（右键按状态显示 + 弹窗内删除）· 持仓监控补搜索/筛选/行内操作 · 修表格 `flex-height` 父级无高度导致整表高度算 0 |
| v1.5.2 | 2026-09-17 | 市场板块成分股新增「分析」入口 · 修复点「详情」后整个板块页面消失（改为弹窗内第三视图） |
| v1.5.1 | 2026-09-16 | 筹码分布下线（拿不到权威数据，宁可不摆）· 监控可停止/恢复 · 自选股「分析」入口固定右侧 · 筛选器横向滚动条修复 |
| v1.5.0 | 2026-09-16 | 支撑/压力位（权重随规则变）· 交易规则按个股状态自动匹配 · 评分因子精简为 4 个独立维度 · 板块中心 |
| v1.4.0 | 2026-09-15 | 策略可自建可管理（内置 5 → 7 套）· 个股操作计划（买点/ATR 止损/止盈/仓位）· 规则历史回测（扣 0.46% 双边成本）· 自选股「分析」入口 |
| v1.3.3 | 2026-09-14 | 图表在窗口缩放后自适应重绘（PR #3）；自选股与筛选结果接上评分排序 |
| v1.3.2 | 2026-09-14 | 修自动更新在国内网络下静默失效：改为先读系统代理（旧端口表漏了 7897） |
| v1.3.1 | 2026-09-14 | 筛选器改为表格级远程分页，修翻页错位与撑出视口（PR #2） |
| v1.3.0 | 2026-09-13 | **四大模块**：全市场筛选器 / 推荐榜 / 个股量化评分 / 智能监控；数据层重写为多通道回退 |
| v1.2.0 | 2026-09-11 | 分时图补上涨跌幅（按昨收算）；分组快捷键改为录制式并做冲突校验 |
| v1.1.0 | 2026-09-11 | 行情时间限制总开关；休市保留最后行情；悬浮条轮播 / 固定双模式；响应式布局 |
| v1.0.0 | 2026-09-10 | 首个版本：在原项目基础上加自选股分组、持仓管理、行情提醒、桌面提醒窗、偏好设置面板 |

> 本项目基于 [Leaderxin/quant-desktop](https://github.com/Leaderxin/quant-desktop) 二次开发，
> 遵循 PolyForm Noncommercial License 1.0.0（**仅限非商业使用**），详见 [LICENSE](LICENSE)。
