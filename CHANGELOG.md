# Changelog

## v2.1.1 (2026-09-18)

本次两件事：**悬浮条上直接管自选股**（不切到主界面就能加 / 删），以及把**本地历史数据引擎
接管起来** —— 设置里选一次引擎，之后随应用自动启停、顶栏一键更新数据；
另外在 README 里正式标注了数据引擎的出处。

### 悬浮窗：不用打开主界面就能加 / 删自选股

- 悬浮条右侧新增 **＋** 入口。它平时是隐藏且**不可点击**的 ——
  整条悬浮条都是「点击打开主界面」的热区，放一个看不见但能点的按钮必然误触，
  所以只在鼠标悬停时才露出来
- 点开后是一个贴着悬浮条弹出的「快速自选」小面板：
  - **加**：输入代码或名称即搜（复用全市场搜索的新浪 → 腾讯 → 数据源三级回退），
    点结果就加入；**回车直接加第一项**，输代码时基本不用碰鼠标
  - **删**：当前自选列表每行一个 ×，一键移除
  - **看**：面板一收起，悬浮条上就有这只票的行情
- 典型用法就是「马上看一眼某只票 → 看完直接删掉」，全程不需要主界面
- **为什么是独立小窗，而不是直接在悬浮条上做**：悬浮条是 `WS_EX_NOACTIVATE` 的
  非激活工具窗 —— 这正是它「点了不抢焦点、像不存在一样」的原因 —— 代价是
  **它拿不到键盘焦点**，上面放输入框是打不了字的。新窗口只在需要时弹出，
  悬浮条原有的拖拽 / 点击 / 不抢焦点行为一行没动
- 面板失焦自动收起，也可以按 `Esc` 或点右上角 ×；增删完悬浮条与主界面都会自动刷新
  （后端本来就发 `watchlist-changed`，两条界面都在监听）

### 本地历史数据：自动管理 stockdb

- 新增独立的 stockdb 管理器：**自动扫描候选 / 手动浏览选择**引擎（`stockdb.exe`）与
  更新程序（`数据更新.exe`），并负责启停与状态上报（`stockdb-status-changed` 事件）
- **默认关闭**：不开就**不启动、不探测、不读取** stockdb，实时行情 / 盘口 / 监控 / 提醒
  一律不受影响。开启后**随应用启动自动拉起**已选的 `stockdb.exe`
- **只管理自己拉起的实例**：关到托盘不停服务；从托盘真正退出时才关闭它。
  如果你已经手工启动了服务，它会直接使用，但**不接管、也不结束**你的进程
- 顶栏设置按钮旁新增「更新本地数据」：先停掉自己启动的 stockdb →
  弹出同目录 `数据更新.exe` 的原生窗口 → 更新程序退出后自动把服务拉回来。
  更新期间按钮转圈并禁用，此时从托盘退出会被拦下并提示等待
- 设置面板里能看到探测结果、候选列表与当前服务地址（默认 `http://127.0.0.1:7899`）
- 平台说明：**当前只有 Windows 支持自动管理**（引擎本身是外部程序，不含在安装包里）
- 为此引入 `tauri-plugin-dialog`（用于选文件），capabilities 补 `dialog:allow-open`

### 文档

- README 补上**本地历史数据引擎**的引用：**[hello245m/free-stockdb](https://github.com/hello245m/free-stockdb)**，
  写法与原作者的署名保持一致（顶部说明 + 致谢段 + 许可证），并写明它是以
  **独立本机服务**的形式对接 —— 本仓库不分发它的代码或数据，是否启用由用户决定
- 原因是这块能力此前只在版本说明里提了一句「可配 stockdb 地址」，没交代它来自哪个项目、
  什么许可，对使用者不够清楚

### 质量

- Rust 单元测试 **214 项：200 通过 / 14 忽略 / 0 失败**
- `vue-tsc --noEmit` 与 `vite build` 均通过
- ⚠️ 新窗口必须同时在 `src-tauri/capabilities/default.json` 的 `windows` 里登记，
  否则它的窗口 / 事件 API 会被能力系统拒绝（悬浮条、提醒窗当年也是这么加的）

## v2.0.0 (2026-09-18)

本次把应用从「看盘 + 量化评分」推到两条新主线上：**让本机的 AI 参与解读**，
以及**让策略能被模拟验证、并持续淘汰**；同时补齐板块中心、盘中资讯、本地历史数据三块。
所有 AI 与模拟功能**默认关闭**，不开就完全不会联网，也不会动到账本。

### 本地 Agent（Claude Code）

- 应用**不直连模型、不管理密钥**：改为驱动本机已装的 Claude Code，输入只有代码算出来的量化快照。
  这样不必维护第二套模型配置，也直接复用你本机已有的 agent 能力
- **权限是显式收敛的**：只用 `--permission-mode`，**明确禁止 `bypassPermissions`**（有测试守着），
  工作目录隔离；**不读取、也不改动** `~/.claude` 的全局配置。固定单并发、90 秒超时、可中断并回收整个进程树
- 检测不到可执行文件时**给明确指引**，而不是静默失败；agent 失败时只保留纯量化结果，不假装有 AI 结论
- 两个入口：单次单角色解读，以及**多子代理团队分析**（技术 / 资金 / 资讯等角色各跑一个进程再汇总）
- 每次预测落库，**次日自动比对**给出真实校准曲线 —— 不靠"感觉准"

### 模拟买卖与胜率测算

- 独立的模拟账户（与真实持仓数据分开）；下单来源分**手动 / 规则 / AI** 三种，**每笔都记触发来源** ——
  否则事后答不出「这个数字是哪条策略跑出来的」
- 成本集中到可配的 `FeeConfig`，**不再是写死的单一常数**
- **只允许用未复权行情成交**：本地历史库目前只有前复权 K 线，这种情况下自动任务**只生成指令、
  不改动账本**。用复权价撮合会把涨跌停判定和成本一起算错，这是最容易埋进去的一个坑
- 拒绝路径覆盖：停牌、涨跌幅限制未知（**宁可阻断也不错误成交**）、一字 / 开盘涨跌停、
  **T+1 可用持仓**、可用资金不足；自动下单按 **100 股整手**取整

### 策略卡片与闭环

- 策略**先登记成卡片才能进入验证**，不许「口头策略」直接跑；每次修改留版本，可回溯
- 六段流水线**不可跳不可省**：静态校验 → 因果审计 → 历史回看 → 准入门槛 → 实时前向 → 考核期；
  **阶段没全部通过，就不允许转成「在用」**
- 策略库固定展示 **试用 / 淘汰 / 在用** 三个计数 —— 试得越多、挑出来的越好看，
  这是多重检验偏差在闭环里的具体形态，必须摆在明面上
- 在用策略可降级 / 暂停；**淘汰只退役不删除**，保留全部历史与每一次状态变更的原因

### 因果审计与因子研究

- 因果审计 = 静态字段 / 算子 / 窗口校验 + **前缀重算对比**（全量结果与「只截到 t」的结果必须一致）。
  此前文档声称「不含未来函数」却没有任何测试证明，现在有了
- 信号实现与操作计划、回测**共用同一份**，避免三处各自漂移
- 新增因子研究：分位收益与正收益概率分布，用来判断一个信号到底有没有预测力

### 板块中心

- 板块 K 线；涨停家数（**明确标注为非官方派生**，算不出来就写「不可用」，不编数）
- 主力净流入 / 净占比、近 3 / 5 / 10 日涨跌幅
- 资金流与轮动视图：强势 / 弱势 × 流入 / 流出四象限 + 批量资金流排序
- 资金流字段本次没返回时如实提示，**不影响**板块排行与成分股

### 盘中资讯（规则版）

- 独立轮询（东方财富上市公司快讯 + 公司公告），**默认关闭，关闭时零请求**
- 交易日按关注池匹配、类型白名单分级；**首次开启只建立当前水位**，不补推历史内容
- 命中后尝试 Agent 摘要，失败回退原文

### 本地历史数据（stockdb）

- 设置里可配服务地址与引擎目录，支持扫描与连通性测试
- 长周期分析**优先读本机 stockdb**；不可用时单股分析回退在线短区间，并**如实标注天数与来源**
  （在线通道 640 根左右就封顶，本地是二十多年，两种来源不能混着说）

### 其他

- NSIS 安装模式固定为 `currentUser`（不再需要管理员权限）
- 新增依赖 `sha2` / `hex` / `rmp-serde`；移除已无引用的 `scripts/make-latest-json.mjs`

### 质量

- Rust 单元测试 **212 项：198 通过 / 14 忽略 / 0 失败**
- `vue-tsc --noEmit` 与 `vite build` 均通过

## 更早的版本

> v1.0.0 – v1.5.3。这些功能都还在产品里，所以每个版本只记一句要点；
> 完整说明与各平台安装包见 [Releases](https://github.com/ChineseCanFly-wxy/bull-arrives/releases)。

| 版本 | 日期 | 要点 |
|---|---|---|
| v1.5.3 | 2026-09-17 | 智能监控可删除（右键按状态显示 + 弹窗内删除）· 持仓监控补搜索/筛选/行内操作 · 修表格 `flex-height` 父级无高度导致整表高度算 0 |
| v1.5.2 | 2026-09-17 | 市场板块成分股新增「分析」入口 · 修复点「详情」后整个板块页面消失（改为弹窗内第三视图） |
| v1.5.1 | 2026-09-16 | 筹码分布下线（拿不到权威数据，宁可不摆）· 监控可停止/恢复 · 自选股「分析」入口固定右侧 · 筛选器横向滚动条修复 |
| v1.5.0 | 2026-09-16 | 支撑/压力位（权重随规则变）· 交易规则按个股状态自动匹配 · 评分因子精简为 4 个独立维度 · 板块中心 |
| v1.4.0 | 2026-09-15 | 策略可自建可管理（内置 5 → 7 套）· 个股操作计划（买点/ATR 止损/止盈/仓位）· 规则历史回测（扣 0.46% 双边成本）· 自选股「分析」入口 |
| v1.3.3 | 2026-09-14 | 图表在窗口缩放后自适应重绘（PR #3）；自选股与筛选结果接上评分排序 |
| v1.3.2 | 2026-09-14 | 修自动更新在国内网络下静默失效：改为先读系统代理（旧端口表漏了 7897） |
| v1.3.1 | 2026-09-14 | 筛选器改为表格级远程分页，修翻页错位与撑出视口（PR #2） |
| v1.3.0 | 2026-09-13 | **四大模块**：全市场筛选器 / 推荐榜 / 个股量化评分 / 智能监控；数据层重写为多通道回退 |
| v1.2.0 | 2026-09-11 | 分时图补上涨跌幅（按昨收算）；分组快捷键改为录制式并做冲突校验 |
| v1.1.0 | 2026-09-11 | 行情时间限制总开关；休市保留最后行情；悬浮条轮播 / 固定双模式；响应式布局 |
| v1.0.0 | 2026-09-10 | 首个版本：在原项目基础上加自选股分组、持仓管理、行情提醒、桌面提醒窗、偏好设置面板 |

> 本项目基于 [Leaderxin/quant-desktop](https://github.com/Leaderxin/quant-desktop) 二次开发，
> 遵循 PolyForm Noncommercial License 1.0.0（**仅限非商业使用**），详见 [LICENSE](LICENSE)。
